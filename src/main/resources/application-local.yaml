spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/orbitra_local?currentSchema=orbitra
    username: erickbarrosr
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

logging:
  level:
    root: INFO
    org.hibernate.type.descriptor.sql: TRACE